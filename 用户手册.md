# YoloMarkFlow 用户手册

> 专业图像标注工具 - 支持 YOLO 训练  
> 本手册基于实际代码编写，所有功能和操作均已实现

---

## 目录

1. [快速开始](#快速开始)
2. [项目管理](#项目管理)
3. [标注工作台](#标注工作台)
4. [数据集管理](#数据集管理)
5. [模型训练](#模型训练)
6. [设置](#设置)
7. [快捷键参考](#快捷键参考)
8. [常见问题](#常见问题)

---

## 快速开始

### 首次使用流程

1. **启动应用**
   - 双击运行 YoloMarkFlow
   - 首次启动显示欢迎界面

2. **创建项目**
   - 点击"新建项目"
   - 输入项目名称
   - 点击"创建"

3. **导入图片**
   - 自动进入标注工作台
   - 点击"导入图片"按钮
   - 选择要标注的图片文件

4. **添加类别**
   - 在右侧面板点击"添加"按钮
   - 输入类别名称（如：cat、dog）
   - 按回车确认

5. **开始标注**
   - 选择一个类别
   - 在画布上拖动鼠标绘制标注框
   - 按 Enter 键保存

---

## 项目管理

### 新建项目

**操作步骤：**
1. 点击左侧菜单"新建项目"按钮
2. 填写项目信息：
   - 项目名称（必填）
   - 项目描述（可选）
3. 点击"创建"

**说明：**
- 项目数据存储在图片池路径中
- 项目信息自动保存

### 打开项目

**操作步骤：**
1. 点击"打开项目"按钮
2. 在列表中选择项目
3. 点击"打开"或双击项目名

**提示：**
- 支持搜索功能快速查找项目
- 最近使用的项目显示在前面

### 切换项目

**操作步骤：**
1. 点击菜单顶部的项目下拉框
2. 选择要切换的项目

**注意：**
- 切换前自动保存当前标注
- 切换后加载新项目数据

### 删除项目

**操作步骤：**
1. 在项目列表中找到要删除的项目
2. 点击删除按钮
3. 选择删除方式：
   - 仅删除项目记录
   - 同时删除所有数据

**警告：**
- 删除操作不可撤销
- 建议先导出数据集备份

---

## 标注工作台

### 界面布局

标注工作台采用三栏式布局：

```
┌──────────────┬─────────────────────────┬──────────────┐
│              │                         │              │
│  图片列表    │      标注画布           │  类别管理    │
│              │                         │              │
│  缩略图      │      绘制区域           │  工具面板    │
│  状态筛选    │      缩放控制           │              │
│              │                         │              │
└──────────────┴─────────────────────────┴──────────────┘
```

### 导入图片

**操作步骤：**
1. 点击"导入图片"按钮
2. 选择图片文件（支持多选）
3. 等待导入完成

**支持格式：**
- JPG / JPEG
- PNG
- BMP
- WebP

**自动去重：**
- 系统通过MD5识别重复图片
- 相同内容只保存一份
- 重复导入会提示

### 图片列表

#### 状态筛选

点击筛选标签可以过滤显示：
- **全部** - 所有图片
- **已标注** - 包含标注框的图片
- **未标注** - 还未标注的图片
- **负样本** - 标记为负样本的图片

#### 缩略图显示

**展开/折叠：**
- 点击"展开"按钮：大图模式，可调整缩放
- 点击"折叠"按钮：紧凑模式

**缩放调整：**
- 展开后显示缩放滑块
- 可调整 50% - 200%

**图片操作：**
- 单击缩略图加载图片
- 右键显示操作菜单
  - 删除图片

### 类别管理

#### 添加类别

**操作：**
1. 点击"添加"按钮
2. 输入类别名称
3. 按回车或点击确认

**命名建议：**
- 使用英文名称
- 名称不能重复
- 避免特殊字符

#### 编辑类别

**修改名称：**
- 双击类别名称进入编辑
- 输入新名称
- 按回车保存

**修改颜色：**
- 点击类别旁的颜色块
- 选择新颜色
- 颜色立即应用到标注框

#### 删除类别

**操作：**
- 右键点击类别
- 选择"删除"
- 确认删除

**警告：**
- 删除不会移除标注框
- 该类别的标注框会失去关联

#### 拖拽排序

**操作：**
- 按住鼠标拖动类别
- 移动到目标位置释放
- 常用类别建议放在前面

### 绘制标注框

#### 基本操作

1. **选择类别**
   - 在右侧列表点击要使用的类别
   - 被选中的类别会高亮显示

2. **绘制矩形框**
   - 在画布上按住鼠标左键
   - 拖动到合适大小
   - 松开鼠标完成绘制

**绘制提示：**
- 可以任意方向拖动
- 按 Esc 键取消绘制
- 绘制时会显示实时预览

### 编辑标注框

#### 选择标注框

**方法：**
- 单击标注框边缘或内部
- 选中后显示蓝色边框和控制点

#### 移动标注框

**操作：**
1. 选中标注框
2. 鼠标移到框内（显示移动光标）
3. 拖动到新位置
4. 松开鼠标

#### 调整大小

**操作：**
1. 选中标注框
2. 鼠标移到四个角或四条边
3. 出现调整光标时拖动
4. 松开鼠标完成调整

**说明：**
- 四个角：同时调整宽高
- 四条边：只调整单一方向

#### 修改类别

**操作：**
1. 选中标注框
2. 在类别列表点击新类别
3. 标注框类别和颜色立即更新

#### 删除标注框

**方法：**
- 选中后按 Backspace 键
- 右键选择"删除标注框"

### 画布操作

#### 缩放画布

**鼠标滚轮：**
- 向上滚动：放大
- 向下滚动：缩小

**按钮控制：**
- 点击 + 按钮：放大
- 点击 - 按钮：缩小
- 使用缩放滑块调整

**快捷键：**
- Plus (+)：放大
- Minus (-)：缩小
- Ctrl+0：重置为 100%

**缩放范围：**
- 最小：10%
- 最大：500%
- 当前比例显示在工具栏

#### 平移画布

**空格键平移：**
1. 按住空格键
2. 鼠标拖动画布
3. 松开空格键

**说明：**
- 按住空格时禁用标注功能
- 松开后恢复正常绘制

### 保存标注

#### 自动保存

**开启方法：**
- 在右侧面板打开"自动保存"开关
- 切换图片时自动保存

**自动保存时机：**
- 切换到其他图片
- 关闭项目
- 退出应用

#### 手动保存

**保存标注：**
- 点击"保存标注"按钮
- 或按 Enter 键

**保存为负样本：**
- 点击"保存为负样本"按钮
- 或按 Ctrl+Shift+S
- 用于标记不含目标对象的图片

**说明：**
- 负样本会删除所有标注框
- 负样本对训练很重要

### 图片导航

**切换图片：**
- 点击"上一张"/"下一张"按钮
- 按方向键 ← 或 →
- 点击左侧缩略图

**跳转功能：**
- Ctrl + →：跳到下一张未标注图片
- Ctrl + ←：跳到上一张未标注图片

**高效标注流程：**
1. 标注当前图片
2. 按 Enter 保存
3. 按 → 切换到下一张
4. 重复以上步骤

### 模型辅助标注

#### 启用条件

**前提：**
- 已完成至少一个模型训练
- 训练任务状态为"已完成"

#### 使用方法

1. **开启模型辅助**
   - 打开右侧的"模型辅助"开关
   - 选择要使用的模型
   - 系统自动对当前图片运行推理

2. **调整置信度**
   - 使用"置信度阈值"滑块
   - 范围：0.1 - 0.9
   - 默认：0.25

**说明：**
- 提高阈值：只显示高置信度结果
- 降低阈值：显示更多检测结果
- 切换图片时自动运行推理

#### 编辑AI标注

AI生成的标注框可以：
- 直接保存使用
- 调整位置和大小
- 修改类别
- 删除错误检测

**建议流程：**
1. 让AI生成初步标注
2. 人工检查修正
3. 保存最终结果

---

## 数据集管理

### 数据集概览

数据集管理页面显示所有项目的标注数据。

**统计信息：**
- 项目列表及图片数量
- 类别分布
- 标注完成情况

### 创建数据集

**操作步骤：**
1. 点击"新建数据集"按钮
2. 输入数据集名称
3. 选择要包含的项目和类别
4. 点击"创建"

**说明：**
- 可以从多个项目组合数据
- 可以选择特定类别
- 自动统计数据量

### 导出数据集

#### 支持格式

1. **YOLO 格式**
   - 适用于 YOLOv5、YOLOv8、YOLO11
   - 每张图片对应一个txt标注文件
   - 自动生成 data.yaml 配置

2. **COCO JSON 格式**
   - 适用于 COCO API、detectron2
   - 单个JSON文件包含所有标注
   - 标准的COCO格式

#### 导出步骤

1. **选择数据集**
   - 在列表中找到要导出的数据集
   - 点击"导出"按钮

2. **配置导出参数**
   - **导出格式**：YOLO 或 COCO
   - **数据划分**：
     - 训练集比例
     - 验证集比例  
     - 测试集比例（可选）
   - **输出路径**：选择保存位置

3. **开始导出**
   - 点击"确认导出"
   - 等待进度完成
   - 查看导出位置

**数据划分说明：**
- 使用滑块调整比例
- 自动随机分配图片
- 确保每个类别在各集合都有分布

**导出路径：**
- 默认使用设置中配置的路径
- 可以临时修改
- 导出文件夹命名：数据集名_v版本_格式

#### YOLO格式说明

**目录结构：**
```
dataset_name_v1_yolo/
├── images/
│   ├── train/      # 训练集图片
│   └── val/        # 验证集图片
├── labels/
│   ├── train/      # 训练集标注
│   └── val/        # 验证集标注
└── data.yaml       # 配置文件
```

**标注格式：**
```
class_id center_x center_y width height
```
- 所有值都是归一化的（0-1之间）
- class_id 从 0 开始

**data.yaml 示例：**
```yaml
path: /path/to/dataset
train: images/train
val: images/val

nc: 2
names: ['cat', 'dog']
```

#### COCO格式说明

**目录结构：**
```
dataset_name_v1_coco/
├── images/          # 所有图片
└── annotations/
    ├── instances_train.json
    └── instances_val.json
```

**JSON格式：**
包含 images、annotations、categories 三个部分

### 数据集版本

**版本管理：**
- 每次导出创建新版本
- 保留历史版本记录
- 可切换到任意版本

**版本操作：**
- 查看版本历史
- 切换版本
- 删除特定版本或全部版本

---

## 模型训练

> **注意**：训练功能需要安装训练插件。请先在设置页面导入训练插件。

### 安装训练插件

**操作步骤：**
1. 从官方渠道下载训练插件压缩包（ZIP 或 RAR）
2. 进入"设置"页面
3. 点击"导入插件"按钮
4. 选择下载的插件压缩包
5. 系统自动解压并安装
6. 重启应用

### 硬件监控

**顶部监控栏显示：**
- CPU使用率
- 内存使用情况
- GPU使用率（如有）

**作用：**
- 确保资源充足
- 避免过热或内存不足
- 根据硬件调整训练参数

### 新建训练任务

#### 配置参数

1. **基本信息**
   - 任务名称
   - 任务描述（可选）

2. **选择数据集**
   - 从数据集列表选择
   - 显示数据集统计信息

3. **模型配置**
   
   **YOLO版本：**
   - YOLOv5
   - YOLOv8
   - YOLO11

   **模型规模：**
   - nano (n) - 最快最小
   - small (s) - 速度精度平衡
   - medium (m) - 推荐
   - large (l) - 高精度
   - xlarge (x) - 最高精度

4. **训练参数**
   
   **Epochs（训练轮数）：**
   - 默认：100
   - 范围：10-500（步长 10）
   - 建议：50-300

   **Batch Size（批次大小）：**
   - 默认：16
   - 范围：1-64（步长 1）
   - 取决于显存大小

   **Image Size（图片尺寸）：**
   - 可选：640、800、1024
   - 默认：640
   - 更大尺寸精度更高但更慢

5. **预训练权重**
   - 使用预训练权重（推荐）
   - 从头训练

6. **高级参数**（可选）
   - 学习率（Learning Rate）：默认 0.01
   - 优化器：SGD、Adam、AdamW
   - Train/Val 比例：默认 80:20
   - 早停（Early Stop）：启用后自动停止训练
   - Patience：5-100（早停轮数）

**参数建议：**

| 数据集规模 | Epochs | Batch Size | Model |
|-----------|--------|------------|-------|
| < 100张   | 50-100 | 8-16       | nano/small |
| 100-1000张| 100-200| 16-32      | small/medium |
| > 1000张  | 200-300| 32-64      | medium/large |

#### 开始训练

**步骤：**
1. 配置完成后点击"开始训练"
2. 显示"正在准备训练数据"加载卡片
3. 系统自动导出数据集到临时目录
4. 如果无运行任务则立即开始训练
5. 否则加入队列等待

**训练队列：**
- 同时只能运行一个任务
- 其他任务在队列中等待
- 当前任务完成后自动启动下一个

### 训练监控

#### 实时进度

**训练卡片显示：**
- 当前 Epoch / 总 Epochs
- 进度百分比
- 预计剩余时间
- 当前状态（运行中/暂停/等待）

#### 训练指标

**主要指标：**
- **Loss（损失）**：应该逐渐下降
- **mAP@0.5**：主要评估指标
  - 50%以上：及格
  - 70%以上：良好
  - 90%以上：优秀
- **Precision（精确率）**
- **Recall（召回率）**

**查看方式：**
- 实时显示在训练卡片
- 点击卡片查看详细指标
- 指标曲线实时更新

### 训练控制

#### 暂停/恢复

**暂停训练：**
- 点击"暂停"按钮
- 当前Epoch完成后暂停
- 状态变为"已暂停"

**恢复训练：**
- 点击"恢复"按钮
- 从暂停点继续训练

#### 停止训练

**操作：**
- 点击"停止"按钮
- 确认停止
- 立即终止训练

**说明：**
- 已训练的权重会保存
- 可以查看部分训练结果

### 训练结果

#### 训练完成

**完成后：**
- 状态变为"已完成"
- 显示最终指标
- 模型文件自动保存

**模型文件位置：**
- best.pt - 最佳模型
- last.pt - 最后一轮模型  
- 保存在训练输出目录

#### 模型评估

**操作：**
1. 点击训练任务的更多菜单
2. 选择"模型评估"
3. 查看详细评估报告：
   - 混淆矩阵
   - PR曲线
   - 各类别性能

#### 导出模型

**操作：**
1. 点击"导出模型"
2. 选择导出位置
3. 下载模型文件

### 训练历史

**右侧面板显示：**
- 所有已完成的训练任务
- 训练时间和配置
- 最终指标

**操作：**
- 查看任务详情
- 重新训练（使用相同配置）
- 模型评估
- 导出模型
- 打开模型目录
- 删除记录

---

## 设置

### 通用设置

#### 主题

**选项：**
- 浅色主题（默认）
- 深色主题

**切换：**
- 在设置页面选择主题
- 立即生效，无需重启

### 路径设置

#### 图片池路径

**说明：**
- 统一存储所有项目图片
- 自动去重功能
- 多项目共享

**修改：**
1. 点击"选择目录"
2. 选择新路径
3. 选择是否迁移数据
4. 重启应用生效

**数据迁移：**
- 确定迁移：复制所有数据到新路径
- 仅更改路径：不迁移，新项目用新路径

#### 默认导出路径

**说明：**
- 数据集导出的默认位置
- 可在导出时临时修改

**修改：**
- 点击"选择目录"
- 选择新路径
- 立即生效

#### 模型下载目录

**说明：**
- 预训练模型下载位置
- 避免重复下载

**修改：**
- 点击"选择目录"
- 选择新路径
- 可选择是否迁移已有文件

### 模型文件

**说明：**
- 存放预训练模型文件（.pt 格式）
- 支持 YOLOv5/v8/11 模型

**目录：**
- 应用目录下 `models/` 文件夹
- 或用户工作空间 `D:\YoloMarkFlow\model`

### 插件管理

#### 导入插件

**操作步骤：**
1. 点击"导入插件"按钮
2. 选择插件压缩包（ZIP 或 RAR 格式）
3. 系统自动解压并安装
4. 查看安装状态

**说明：**
- 插件会自动解压到 `plugins/` 目录
- 系统自动扫描可用插件
- 支持训练插件、推理插件等

#### 插件列表

**显示信息：**
- 插件名称和版本
- 插件类型和描述
- 插件状态

**操作：**
- 刷新插件列表
- 查看插件详情

---

## 快捷键参考

### 标注工作台快捷键

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| Enter | 保存当前标注 | 保存当前图片的所有标注框 |
| Backspace | 删除标注框 | 删除选中的标注框 |
| Ctrl+Shift+S | 保存为负样本 | 标记图片为负样本并删除标注 |
| → | 下一张图片 | 切换到下一张图片 |
| ← | 上一张图片 | 切换到上一张图片 |
| Ctrl+→ | 跳到下一张未标注 | 快速找到未标注图片 |
| Ctrl+← | 跳到上一张未标注 | 快速找到未标注图片 |
| Plus (+) | 放大画布 | 放大显示 |
| Minus (-) | 缩小画布 | 缩小显示 |
| Ctrl+0 | 重置缩放 | 恢复100%显示 |
| Space（按住）| 平移画布 | 按住空格拖动平移画布 |

**快捷键自定义：**
- 所有快捷键可以自定义
- 在工具栏打开"快捷键设置"对话框
- 点击要修改的快捷键
- 按下新的按键组合
- 自动检测冲突

**使用说明：**
- 在输入框中不会触发快捷键
- 快捷键设置对话框中不会触发
- 支持修饰键组合（Ctrl、Shift、Alt）

### 鼠标操作

| 操作 | 功能 |
|------|------|
| 滚轮滚动 | 缩放画布 |
| 左键拖动 | 绘制标注框 |
| 左键点击 | 选择标注框/类别 |
| 右键点击 | 显示上下文菜单 |
| Space+拖动 | 平移画布 |

---

## 常见问题

### 标注相关

**Q: 为什么标注框绘制不出来？**

A: 检查以下几点：
1. 是否选择了类别（右侧类别列表应该有高亮）
2. 是否加载了图片
3. 画布是否处于正常模式（没有按住空格键）

**Q: 如何修改标注框颜色？**

A: 点击类别旁边的颜色块，在颜色选择器中选择新颜色。

**Q: 误删除了标注框怎么办？**

A: 目前没有撤销功能，建议：
- 重新绘制
- 或者不保存直接切换到其他图片（如果还没保存）

**Q: 图片加载很慢？**

A: 建议：
1. 压缩图片到合适尺寸（1920x1080以内）
2. 降低缩略图显示比例
3. 关闭其他应用释放内存

### 数据集相关

**Q: 导出的数据集可以直接训练吗？**

A: 可以。导出的YOLO格式数据集符合YOLOv5/v8/v11标准，可以直接用于训练。

**Q: 如何导入其他工具的标注数据？**

A: 目前支持导入YOLO和COCO格式的数据集。在数据集管理页面使用"导入"功能。

**Q: 导出时提示部分图片没有标注？**

A: 这是警告信息，不影响导出。可以：
- 返回补充标注
- 将无标注图片标记为负样本
- 或继续导出（只会导出已标注的图片）

### 训练相关

**Q: 如何获取训练插件？**

A: 训练插件需要单独获取：
1. 从官方渠道下载训练插件压缩包
2. 在设置页面点击"导入插件"
3. 选择下载的压缩包
4. 系统自动安装
5. 重启应用即可使用

**Q: 训练速度很慢？**

A: 解决方法：
1. 确认是否使用GPU（查看硬件监控）
2. 降低Batch Size
3. 减小图片尺寸（如640→320）
4. 选择更小的模型规模

**Q: GPU显存不足（Out of Memory）？**

A: 解决方法：
1. 降低Batch Size（如32→16→8）
2. 减小图片尺寸
3. 关闭其他占用GPU的程序
4. 选择更小的模型（如medium→small→nano）

**Q: 训练中断了怎么办？**

A: 训练会自动保存checkpoint。查看训练历史，如果任务显示"已暂停"或"失败"，可以尝试恢复训练。

**Q: mAP很低怎么办？**

A: 可能原因和解决方法：
1. **数据量不足**
   - 每个类别至少100张以上
   - 使用数据增强

2. **数据质量问题**
   - 检查标注准确性
   - 删除模糊图片
   - 确保类别数据均衡

3. **训练参数不合适**
   - 增加Epochs数量
   - 尝试不同模型规模
   - 使用预训练权重

4. **目标太小**
   - 增大训练图片尺寸（640→1280）
   - 使用large或xlarge模型

### 性能相关

**Q: 应用运行卡顿？**

A: 优化方法：
1. 关闭不必要的应用
2. 使用状态筛选减少显示图片数量
3. 降低缩略图缩放比例
4. 重启应用清理内存

**Q: 打开大项目很慢？**

A: 这是正常现象。图片很多时第一次加载会较慢，系统会缓存，第二次会快很多。

### 数据安全

**Q: 数据存储在哪里？**

A:
- 图片：图片池路径
- 标注：图片池路径的数据库文件
- 项目配置：应用数据目录

**Q: 如何备份数据？**

A:
1. 定期导出数据集
2. 备份整个图片池目录
3. 备份应用数据目录（可在设置中查看路径）

**Q: 误删除项目能恢复吗？**

A:
- 如果选择"仅删除项目记录"：数据还在，可以重新创建项目并导入
- 如果选择"同时删除所有数据"：无法恢复

---

## 附录

### 系统要求

**最低配置：**
- CPU：Intel i5 / AMD Ryzen 5
- 内存：8GB
- 硬盘：10GB可用空间
- 操作系统：Windows 10+

**推荐配置：**
- CPU：Intel i7 / AMD Ryzen 7
- 内存：16GB
- 硬盘：50GB SSD
- 显卡：NVIDIA GTX 1660+（6GB显存）
- 操作系统：Windows 10+

**最佳配置：**
- CPU：Intel i9 / AMD Ryzen 9
- 内存：32GB+
- 硬盘：500GB NVMe SSD
- 显卡：NVIDIA RTX 3080+（10GB显存）
- 操作系统：Windows 11

### YOLO版本对比

| 版本 | 发布时间 | 特点 | 推荐场景 |
|------|---------|------|---------|
| YOLOv5 | 2020 | 成熟稳定，社区资源丰富 | 生产环境 |
| YOLOv8 | 2023 | 精度提升，训练更快 | 新项目首选 |
| YOLO11 | 2024 | 最新版本，性能最优 | 追求极致性能 |

### 模型规模对比

| 规模 | 参数量 | 速度 | 精度 | 适用场景 |
|------|--------|------|------|---------|
| nano | ~2M | 最快 | 较低 | 移动端、嵌入式设备 |
| small | ~9M | 快 | 中等 | 边缘设备、实时应用 |
| medium | ~26M | 中等 | 良好 | 个人电脑、服务器 |
| large | ~53M | 较慢 | 高 | GPU服务器 |
| xlarge | ~97M | 最慢 | 最高 | 高性能GPU集群 |

### 术语表

- **标注（Annotation）**：在图片上标记目标对象
- **标注框（Bounding Box）**：矩形框标记
- **类别（Class）**：目标对象分类
- **数据集（Dataset）**：图片和标注的集合
- **训练集（Train）**：用于训练模型
- **验证集（Validation）**：用于评估模型
- **测试集（Test）**：用于最终测试
- **Epoch**：在整个训练集上训练一遍
- **Batch Size**：一次训练的样本数量
- **mAP**：平均精度均值，主要评估指标
- **Precision**：精确率
- **Recall**：召回率
- **IoU**：交并比，衡量框的重合度
- **负样本**：不含目标对象的图片
- **迁移学习**：使用预训练模型起步


